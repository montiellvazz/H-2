/**
 * mediante el loop for agregar los números 1,3,5,7
 * dentro del array result 
 * 
 * 
 * output => [1,3,5,7]
 */
let result = [];

for(idx = 0; idx <= 7; idx++){
    idx ++
    result.push(idx);
}


//export result
module.exports = result;