/**
 * mediante el loop for agregar los números 7,5,3,1
 * dentro del array result 
 * 
 * 
 * output => [7,5,3,1]
 */
let result = [];

for(idx = 8; idx >= 1; idx --){
    idx --;
    result.push(idx);
}


//export result
module.exports = result;